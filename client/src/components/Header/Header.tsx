import { Button, TomatoButton } from './Header.styled'

const Header = () => {
  return (
    <>
      <Button> laa</Button>
      <TomatoButton>tomatio</TomatoButton>
    </>
  )
}

export default Header
